# O arquivo bootstrap.yml é carregado antes do application.yml.
# Utilizado para obter as configurações que estão no config-server-repository no github
spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          # Acessando Local
#          uri: C:\Cursos\event_driven_microservices_spring_boot_kafka_elastic\microservices-demo-final\config-server-repository
          # Acessando pelo github
          uri: https://github.com/marconunesrj/spring-cloud-config-server-repository
          default-label: main  # branch que está o repositório no github
          username: marconunesrj
          # Utilizando Jasypt (módulo playgroud)
          password: ENC(UEhs1JASfzr2m0lDEWGIZkOl2Qiw6Su5qQz5j4KnAPM+KdmAo9C+f4EJx5G2EHtJpKJEcB2XG0cEyA0D+JibIejNAkejAIdhHQs0VvF4qiE=)
          # Utilizando JCE
    #      password: '{cipher}2ca52e09ec85aa620abce2e704242985bf786c9a684461b023a563b34b96c527ec81160eae48d731017c98ee0b9e1824'
          # Significa que os servidores de configuração clonam o repositório do servidor de configuração na inicialização
          clone-on-start: true
    # Significa que, se o servidor de configuração não conseguir acessar o repositório do servidor
    # de configuração e carregar a configuração, ele vai parar imediatamente
    fail-fast: true
  security:
    user:
      # Mesma informação no arquivo bootstrap.yml do módulo: twitter-to-kafka-service
      name: spring_cloud_user
      # Mesma informação no arquivo bootstrap.yml do módulo: config-server
      # Utilizando Jasypt (módulo playgroud)
      password: ENC(cjAiHZVq89MieOC9iFlfLh5PsECy2vCEkcC5O4UV1itb0NL+MUPkK20O7PeAkeh9VJkbupKk8MTWpeo+ZvPvMw==)
      # Utilizando JCE
#      password: '{cipher}2ca52e09ec85aa620abce2e704242985bf786c9a684461b023a563b34b96c527ec81160eae48d731017c98ee0b9e1824'

jasypt:
  encryptor:
    # https://www.profissionaisti.com.br/o-que-e-e-como-setar-uma-variavel-de-ambiente-linux-e-windows/
    # Executar PowerShell como Administrador
    # Variável de ambiente Temporária (Apenas na Sessão) Execute: set JASYPT_ENCRYPTOR_PASSWORD="Demo_Pwd!2024"
    # Variável de ambiente Permanente (Usuário Atual) Execute: setx JASYPT_ENCRYPTOR_PASSWORD "Demo_Pwd!2024" ou /M
    # Para visualizar as variáveis de ambiente no PowerShell, você pode usar o seguinte comando: Get-ChildItem Env:
    password: ${JASYPT_ENCRYPTOR_PASSWORD}